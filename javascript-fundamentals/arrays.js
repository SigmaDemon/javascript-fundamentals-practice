// Arrays - these are useful to store information to be used for later
const schedule = ['Wake Up', 'Eat', 'Code', 'Watch Netflix'];

// Built-In Functions
schedule.push('Wash Laundry'); // adds another element to the end of the array
schedule.pop();  // removes the last element from the array

schedule.indexOf('Eat'); // This accesses the index nmber of the element specified in the brackets

console.log(schedule);
